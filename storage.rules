rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Phase 1+: tighten rules per docs/ARCHITECTURE.md
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
